# Changelog

本项目遵循语义化版本号。

## [2.16.0] - 2026-09-11

### Added

- 增加可替换的 AI Provider 架构，预留直连、中转服务和本地模型三种接入方式。
- 增加 Windows DPAPI 加密的 API Key 存储和结构化 AI 建议校验；默认关闭联网，AI 不直接执行桌宠动作或修改文件。
- 优化桌面贴图窗口：移除无用的贴图序号，增加明显边框与玻璃卡片视觉，并在显示前完成原位置预定位，减少左上角闪现。

## [2.15.1] - 2026-09-11

### Fixed

- 将皮肤 SDK 校验命令加入 Release CI，保证模板和校验入口持续可用。

## [2.15.0] - 2026-09-11

### Added

- 诊断窗口增加资源摘要与配置备份状态，可从诊断窗口恢复最后一次有效配置并保留恢复前文件。
- 新增 `skin-sdk` 模板、皮肤格式入口和 `scripts/validate-skin.ps1` 校验命令，支持目录中的 `pet.json` 与单角色 ZIP。

## [2.14.1] - 2026-09-11

### Fixed

- 更新运行时换肤回归测试，使其验证统一精灵缓存，不再依赖已移除的角色窗口重复帧缓存字段。

## [2.14.0] - 2026-09-11

### Fixed

- 配置主文件损坏时自动恢复最后一次有效的 `.bak`，不再静默创建全新默认配置；主文件和备份都会保留不被覆盖。
- 移除角色窗口重复持有的帧缓存，统一使用精灵缓存，降低多角色和长时间运行时的图片内存占用。
- 诊断报告增加受控的托管内存、进程私有内存、精灵图租约、角色数量、贴图数量和截图状态信息。

## [2.13.1] - 2026-09-11

### Fixed

- 将截图工具栏改为主题自适应的半透明玻璃悬浮层，减少纯白留空并保留深浅主题对比度。
- 为编辑中的马赛克区域增加仅编辑可见的双层辅助边框，导出图片不包含该边框。

## [2.13.0] - 2026-09-11

### Added

- 将截图中的实色遮挡替换为可调强度的马赛克遮挡，支持实时预览并进入复制、保存、贴图和撤销历史。
- 截图工具栏采用苹果 Pro 分组布局，按标注、编辑、输出区分操作，并强化当前工具和完成操作的视觉状态。

## [2.12.2] - 2026-09-11

### Fixed

- 修复快捷键 UI 回归测试在检查“未保存草稿为空”之前先修改测试数据，导致 CI 误报失败的问题。

## [2.12.1] - 2026-09-11

### Fixed

- 修复快捷键设置窗口中只读手势显示框默认使用双向绑定，导致窗口加载时抛出 `XamlParseException` 的问题。

## [2.12.0] - 2026-09-10

### Added

- 截图贴图会保留原截图选区的位置，并在显示器工作区边界内自动修正。
- 快捷键设置支持点击输入框后直接录入组合键，包括微信风格的 `Alt+A`，并支持清除快捷键。
- 截图工具栏按钮和点击区域放大，工具栏宽度自适应更大的微信式布局。

## [2.11.4] - 2026-09-10

### Fixed

- 为高 DPI 托盘菜单设置基于所有菜单行的真实最小高度，避免后续原生布局将菜单压缩并裁掉底部入口。

## [2.11.3] - 2026-09-10

### Fixed

- 修复 192 DPI 下托盘菜单完成真实布局测量后再次布局导致高度被压回、最后一行越界的问题。
- 保留 CI 失败时的逐行边界诊断，便于后续定位不同 DPI 环境的原生菜单布局差异。

## [2.11.2] - 2026-09-10

### Fixed

- 修复原生 UI 回归在慢速 Windows runner 上固定等待时间不足导致的开关模板偶发失败；测试现在等待布局稳定后再检查滑块位置。
- 保留 v2.11.1 的 192 DPI 托盘菜单实际边界兜底修复；v2.11.1 标签不重写。

## [2.11.1] - 2026-09-10

### Fixed

- 修复 GitHub Actions Windows runner 在 192 DPI 下托盘菜单行高按理论值计算，导致最后一行越界或行间重叠的问题；菜单现在在布局完成后按实际控件边界补足尺寸。
- 将 v2.11.0 的原地微信式截图、当前屏幕/全部屏幕入口和完成提醒修复纳入本次 bugfix 发布；旧的 `v2.11.0` 标签不重写。

## [2.11.0] - 2026-09-10

### Added

- 内置区域截图、当前屏幕截图和全部屏幕截图，从角色菜单、控制中心、托盘或独立可配置快捷键启动。当前屏幕按调用时的鼠标位置选择，整屏模式直接预选完整像素范围。
- 截图采用桌面原地浮层：框选松手后保留绿色边框、八个手柄和尺寸提示，可拖边、拖角、移动选区；支持扩大选区及撤销，不默认打开独立编辑窗口。
- 悬浮工具栏跟随选区并避让屏幕边缘，支持矩形、椭圆、箭头、画笔、原地文字、实色遮挡、撤销/重做、复制、PNG 保存和贴图。颜色、线宽与字号按工具展开；工具栏和输入框不进入导出图片。
- 完成复制、保存或贴图成功后退出全部截图浮层；保存取消、剪贴板占用、写入失败和贴图容量不足时保留当前会话。导出为单张扁平位图，不包含原图图层或选区外内容。
- 桌面贴图支持拖动、缩放、置顶、复制、保存和关闭；可从剪贴板图片创建，托盘可统一显示、隐藏和关闭。贴图仅保留在本次运行内存中，不自动保存或随重启恢复。
- 专注或休息完成后保留一个待查看标记；计时窗口使用任务栏角标，后台以托盘图标备用。查看计时面板、下一次交互或开始下一段后清除，不自动开始下一段、不自动截图，也不额外弹出计时窗口。

### Safety

- 截图只有手动触发才读取屏幕；不上传、不自动保存历史、不监视剪贴板，也不把图片加入诊断包。截图期间暂缓本程序通知气泡；显示器变化、锁屏、取消与关闭会结束框选。
- 遮挡使用向外对齐整像素的实色黑块，PNG 和剪贴板输出为合成像素；分享前仍应检查遮挡范围。原地标注期间保留完整快照以便调整选区，结束后不自动保存截图历史。
- 单次屏幕包围矩形最多 3200 万像素，最多 8 张贴图且合计不超过 4800 万像素；撤销最多 100 步、标注最多 300 个，画笔采样设有上限。
- 勿扰和关闭通知时不生成待查看角标；历史记录仍保存，重启不补发旧提醒。三个截图快捷键默认关闭，原有快捷键不变；本版不依赖 Snipaste 或微信。

### Tests

- 覆盖选区坐标、像素一致性、裁剪与遮挡合成、撤销边界、保存、贴图容量与生命周期、窗口临时隐藏恢复、取消和退出，以及计时期间截图和提醒确认的交互。
- 原生界面覆盖深浅主题、浮层工具栏避让、原地文字、选区调整、整屏范围、失败恢复、贴图和窗口回收。真实屏幕读取需要交互桌面权限，受限环境明确跳过该项，继续执行合成图像与窗口模型测试。

## [2.10.0] - 2026-09-10

### Added

- 全局快捷键可配置：召回全部角色、打开专注计时、开始/暂停计时和切换迷你模式。入口位于控制中心“设置 → 应用 → 快捷键”和角色常规设置；支持独立启用、禁用、恢复默认、保存和取消。
- 保存前检查重复组合与系统占用；注册或配置保存失败时释放临时注册，保留原配置与有效快捷键。启动时单个组合被占用不会阻止其他快捷键或程序启动。
- 菜单提示同步实际配置；重新绑定或退出后丢弃旧队列消息。快捷键设置期间不执行快捷键，角色设置或其他模态窗口期间不触发计时操作。

### Changed

- 控制中心、角色设置、记录、皮肤编辑、备份、介绍、诊断和弹窗统一系统字体及共享标题层级；保留原生标题栏、现有主题、强调色和紧凑布局。
- 统一表单与按钮字体、链接间距，补足图标按钮、滑条和列表行的键盘焦点边界；焦点与悬停不改变控件尺寸。专用等宽字体与图标字体保留。

### Compatibility

- 旧配置默认仅启用 Ctrl+Alt+Y；三个新操作默认关闭。设置保存在既有 desktop-v2.json 的可选 Shortcuts 字段，不升级桌面配置版本，不改变计时、历史和角色素材。
- 支持至少两个 Ctrl/Alt/Shift 修饰键与字母、数字、空格或 F1-F11，不提供 Win 组合、裸键或脚本命令。使用 Windows RegisterHotKey，不安装全局键盘钩子，不记录输入内容。

### Tests

- 增加组合校验、事务回退、命令互换、启动失败重试、旧消息丢弃、真实 Windows 冲突与按键释放、模态保护、时长草稿保护和无面板计时测试。
- 界面检查覆盖深浅主题、默认与最小窗口、滚动可达性、保存/取消/恢复默认和焦点边界；沿用皮肤、计时、截图与资源质量检查。

## [2.9.0] - 2026-09-10

### Changed

- 将学习陪伴、学习记录统一命名为“专注计时”“专注记录”，同步角色菜单、托盘、控制中心、设置、记录提示和使用说明；工作、学习、阅读共用既有专注与休息流程。
- 迷你计时面板去掉角色姓名，顶部以半粗体显示“专注/休息”，同排保留小头像与次要状态；倒计时左对齐，改用 34 号常规字重和等宽数字。默认 320x104，最小 280x104，保留置顶、展开、关闭、开始/暂停、重置、拖动和缩放。
- 完整计时面板弱化角色信息，姓名改为头像悬停提示，同时保留无障碍名称、完成次数、勿扰状态和原始动画反馈。
- 仅计时面板使用 Segoe UI / 微软雅黑 UI 系统字体，完整面板数字同步取消超细字重；其他窗口字体不变，不增加外部字体依赖。

### Compatibility

- 只调整界面和文案，不改变计时规则、历史文件名与格式、已有记录、时长、位置及置顶设置，不修改任何角色素材。已有保存的窗口尺寸继续保留，可手动拖到新的最小高度；新默认尺寸仅用于尚未保存迷你尺寸的配置。

### Tests

- 覆盖完整/紧凑/迷你布局、最小和最大尺寸、最长倒计时、阶段切换、角色提示更新、菜单命名和深浅主题；随本次界面调整复核并更新六张计时器截图基线。

## [2.8.0] - 2026-09-10

### Added

- 诊断窗口增加活动采样页，默认不采样，手动开始后显示角色状态、动作、活动计时器、回调计数、帧回调超期 P95 和最近规则决策。
- 采样最多 5 分钟，状态变化和延迟样本使用有上限的内存队列；停止、超时、关闭及角色移除时解除订阅。诊断窗口改为单实例非模态窗口，可从控制中心打开后继续操作角色。
- 诊断导出可包含本次采样快照，使用临时角色编号及固定枚举，不含角色名称、皮肤 ID、源文件路径、鼠标坐标或原始规则 JSON；仍只生成既有三个诊断包文件，不自动上传。
- `pet.json` 增加可选 `behavior` 字段，支持任意位置、鼠标靠近和鼠标远离条件，以及有限动作的权重与独立冷却；不执行脚本，不提供系统优先级覆盖。
- 动作编辑器增加行为规则页，支持添加、删除、修改条件与动作、权重和冷却，沿用撤销/重做、严格校验、保存、导出与源文件刷新。

### Compatibility

- 不含行为规则的旧皮肤继续使用原来的随机待机选择。空规则表表示禁用自定义自动动作，不偷偷退回默认规则。
- 规则仍受随机动作开关、全局间隔、皮肤能力、拖动、菜单、设置、隐藏、贴边、勿扰和专注抑制保护。
- 只允许不循环且总时长不超过 30 秒的打招呼、跳跃和失败动作；最多 9 条规则，权重 1-100，冷却 5-3600 秒，禁止重复条件/动作组合、未知字段和非法枚举。
- 原始 PNG 和随附角色清单不变；含 `behavior` 的皮肤需要 v2.8.0 或更新版本，旧版兼容包应省略该可选字段。

### Tests

- 增加规则解析边界、权重选择、冷却保持、受保护状态、编辑历史、空规则、PNG 字节一致性及原生角色采用规则测试。
- 增加手动采样开关、多观察者、编号稳定性、退出记录、内存队列上限、五分钟超时、导出脱敏和深浅色小窗口布局测试。

## [2.7.0] - 2026-09-10

### Changed

- 增加 `PetBehaviorController`，集中管理拖动、菜单、游走、注视、动作归属及活动权限；窗口仅采集原生状态、执行渲染和位置更新。
- 桌面精灵采用单调时钟播放：按累计帧期限定位当前帧，延迟回调跳过过期帧，不再每次回调只前进一帧；非循环动作完整展示末帧时长后结束。
- 隐藏、收纳和注视期间暂停精灵播放时钟，恢复时保留未完成帧的剩余时长。静态单帧循环皮肤不启动帧计时器。
- 自动动作与走动调度改用单调时钟，系统时间校正不会使其提前触发或回退；移动恢复仍限制每步最多补偿 100ms，避免长时间卡顿后瞬移。
- 只有启用鼠标注视时保持约 100ms 的观察周期；仅随机动作或自动走动时直接等待下一期限。勿扰、专注抑制、菜单、拖动及设置期间停止不适用的自动定时器。
- 鼠标靠近导致走动暂停时降低检查频率；贴边状态未变化时不重复更新窗口边界和标签；同一动画帧不重复设置图像源。
- 原始 PNG、动作参数、窗口布局和配置格式保持不变。本轮没有插帧、重绘，也没有合并所有原生计时器。

### Tests

- 增加帧期限、迟到回调、隐藏恢复、长时间停顿、系统时间变化、单帧皮肤、状态优先级及游走积分测试。
- 增加 16 个固定种子、共 64,000 次行为切换检查；失败记录种子、步骤与最近操作。
- 增加三角色原生回归，验证隐藏、勿扰、菜单、专注、手动动作结束和贴边状态下的活动权限与原始帧渲染。
- 新增同场景前后性能探针，记录角色回调数、帧间隔、分配量和进程 CPU；短样本结果不等同于电池或 GPU 功耗，详见 `docs/ACTIVITY_PERFORMANCE.md`。

## [2.6.0] - 2026-09-09

### Added

- 动作编辑器增加撤销/重做，保留无效输入、动作开关和逐帧参数；连续输入合并为一步，历史最多 100 个检查点并限制内存，不缓存 PNG 副本。
- 增加上一帧/下一帧和按实际帧时长定位的时间轴，定位后暂停；支持批量设置全部帧或所选帧时长，一次批量修改只占一步撤销。
- 编辑器增加当前皮肤源文件自动刷新、单次手动检查及打开源文件夹入口。后台合并文件事件，连续两次完整快照一致且验证通过后才替换预览；窗口关闭取消监听与后台结果。
- 源文件存在未保存草稿冲突时，可载入外部版本或保留草稿另存/导出。损坏、半写入、文件锁定或皮肤 ID 变化不会清空上次可用预览。

### Changed

- 无效本地输入仍保留最后可用的角色预览，同时禁用保存及播放控制；撤销可恢复可编辑状态。
- 保存更新检查源文件指纹，并在替换目录后重新确认，避免覆盖外部修改；保留已有备份机制与随附皮肤保护。
- 热重载仅作用于当前编辑器预览；桌面角色仍由设置页保存后应用。不监听原始下载目录，不后台覆盖桌面状态，不修改原始 PNG 像素。
- 载入外部版本清空旧版本编辑历史，防止撤销旧动作参数到不同精灵图；重置恢复最近载入的源配置。

### Tests

- 补充历史合并、撤销分支、内存上限、无效输入恢复、批量选择和时间轴帧边界测试。
- 补充后台快照跨线程取帧、快速文件事件、锁定/半写入/损坏文件、路径越界、目录替换、ID 冲突、关闭取消及深浅色小窗口布局测试，纳入既有 CI 原生 UI 回归。

## [2.5.0] - 2026-09-09

### Changed

- 学习陪伴按职责拆分：`CompanionService` 负责计时、通知、记录与持久化，`CompanionRuntime` 保留 WPF 调度和窗口生命周期，`FocusPanelController` 管理编辑草稿、命令与显示快照。
- 保留完整/紧凑/迷你窗口外观、调时防抖、计时规则及现有配置格式；输入测试通过控制器的公开接口验证，不再反射修改窗口编辑字段。
- 勿扰时段与学习日期使用同一个注入时钟，便于确定性测试；正常运行继续使用系统本地时间。

### Tests

- 固定种子的 128,000 次随机操作与独立的剩余时间模型逐步对照，覆盖暂停、跳过休息、配置变化、休眠和系统时间变化；失败保留种子及最近操作，支持重放。
- 新增无窗口的控制器/服务契约测试，以及带原生窗口的重复打开、模式切换、最小化和关闭检查；检测未释放的窗口、控制器及内存/句柄增长。
- 对比重构前保存的 6 张深浅色界面基线；固定客户端尺寸、关闭装饰动画，允许小幅渲染差异，不自动更新基线。
- 为计时快照、服务轮询、缓存取帧及显示格式化设定耗时/分配预算，输出本地 JSON 报告；支持可配置时长的生命周期循环。
- Build 和 Release 工作流加入质量门禁；失败时保留报告及实际渲染图，未通过时不上传 Release 应用包。详见 `docs/QUALITY_GATES.md`。

## [2.4.0] - 2026-09-09

### Added

- 迷你悬浮钟：与完整圆环复用同一个窗口和计时器，支持开始、暂停、继续、重置、拖动、缩放及独立置顶。
- 完整与迷你模式分别记忆尺寸和位置，重开恢复上次模式；旧配置默认完整模式，保存失败不切换模式。
- 桌面右键菜单增加只读角色介绍入口；角色关闭增加二次确认，不影响其他角色、皮肤库和学习计时。

### Changed

- 桌面菜单按常用入口、动作、显示与行为、角色操作和退出分组，使用紧凑的图标、圆角与主题色样式。
- 菜单关闭立即解除主题订阅；嵌套动作项继续按皮肤能力禁用。确认窗口限制到屏幕可用区域。
- 迷你模式切换遵循减少动态效果设置，不改动皮肤图片或已有计时规则。

### Tests

- 增加配置迁移、模式切换、失败回滚、暂停保持、迷你尺寸与按钮边界、菜单子项、主题同步、角色介绍及关闭确认测试。

## [2.3.0] - 2026-09-08

### Added

- 离线角色台词气泡：点击、拖动结束、专注完成及休息完成触发，支持总开关、全局冷却和每个皮肤的个人台词。
- `pet.json` 可选 `speech` 字段；旧皮肤默认使用通用台词，个人覆盖优先于内置台词，不重绘 PNG。
- 学习记录窗口：今日完成量、每日目标、近 7 天柱图及记录范围筛选；计时面板增加今日总量与记录入口。
- 学习陪伴采用旋钮番茄钟面板：拖动圆环、数字输入、独立专注/休息切换、常用时长及键盘微调；跟随主题、强调色和减少动态效果设置。
- 自适应学习窗口：支持原生四边缩放、最大化及空白区域拖动，提供紧凑和正常布局；圆环按可用空间调整且保持尺寸上限。
- `study-history.json` 独立保存已完成专注、完成时的本地日期及每日目标，原子写入并保留 `.bak`；同一场专注只记录一次。

### Fixed

- 修复角色预览和动画帧偶发“调用线程无法访问此对象”崩溃：后台解码的 `BitmapFrame` 即使冻结，仍保留解码器的线程关联；共享缓存改为独立只读像素位图，再按需生成帧，保留原始像素格式、调色板、透明度和 DPI，不修改 PNG 文件。
- 自动化测试日志改为独立临时目录，避免模拟故障混入用户运行日志；已有日志不清理、不改写。

### Behavior

- 气泡不抢焦点、不拦截鼠标、不改变宠物尺寸；移动、隐藏、停靠、勿扰时不残留，同一时刻最多显示一个。
- 冷却中的台词不排队补发；完成台词同时遵守通知设置，无可用角色时只保留计时反馈和学习记录。
- 取消与休息不计为专注；暂停不累计，跨日按完成时的本地日期计入，休眠最多完成当前一段。退出后不恢复未完成计时。
- 记录读取损坏不自动覆盖，保存失败保留本次内存记录并提示重试；诊断包不包含学习历史或个人台词配置。
- 面板时长即时保存，连续键盘微调合并写入，失败恢复原值；运行与暂停时锁定调时，完成后可准备下一阶段，重置不影响已有学习记录。
- 正常窗口尺寸与屏幕坐标保存在原有配置；拖动合并写入，最小化/最大化不覆盖正常尺寸，显示器或 DPI 改变后重新限制到可用工作区。旧配置兼容，失败保留上次有效值。
- 紧凑窗口收起次要统计但保留主要操作；计时开始时收起预设空间，不自动改变窗口大小。支持时统一标题栏底色，保留系统窗口操作。

### Tests

- 增加台词校验、冷却与勿扰、跨 DPI 气泡位置、被动窗口样式、暂停与跨日统计、重复计数和文件故障恢复测试。
- 增加圆环跨零点、数字范围、时长持久化与失败回滚、阶段切换、暂停保护、无障碍范围操作、窗口主题与紧凑布局检查。
- 增加多尺寸深浅色截图、布局边界、拖动命中区域、编辑草稿保持、窗口位置恢复、异常坐标、最小化/最大化保护及配置保存失败检查。
- 补充后台首次解码后退出线程、UI/后台双向取帧、垃圾回收后刷新、重复打开“关于”页、六种 PNG 像素格式和逐像素一致性回归；冷启动路径在缓存预热前测试。

## [2.2.0] - 2026-09-08

### Added

- 控制中心与角色设置“关于”新增诊断入口，可查看运行信息、脱敏日志并导出本地 ZIP；不自动上传。
- 日志按 Roaming、Local、Temp 顺序尝试写入，所有目录不可写时保留有大小上限的内存记录；显示真实写入状态和当前日志位置。
- 记录启动阶段和关键窗口操作；日志运行期间滚动，单条记录、磁盘文件及诊断包日志均有大小限制。
- 首次启动、旧版迁移、补丁升级、日志写入故障、异常重入、脱敏和导出失败测试。

### Fixed

- WPF 与托盘致命错误共用单次处理入口：先停止活动并清理资源，再只显示一次错误提示，避免错误窗口叠加。
- 致命错误退出不写回异常中的配置，普通退出仍保存；单个清理步骤失败不阻止其余资源释放。
- 启动时的单实例初始化错误也进入诊断流程；退出后忽略迟到的激活请求。

### Notes

- 诊断包只包含允许列出的运行字段及脱敏文本，不包含原始配置、精灵图、截图或环境变量清单。自动脱敏不能保证识别所有自由文本，分享前应检查内容。
- 此版本增强可靠性与故障定位能力；此前未复现的崩溃仍未确认根因，不列为已修复。

## [2.1.2] - 2026-09-08

v2.1.1 因 CI 阴影断言失败未生成 Release，本版包含其托盘菜单改进并修正测试。

### Changed

- 托盘右键菜单改为圆角、轻描边和系统阴影的分组菜单，统一文字对齐、行高与柔和悬停高亮。
- 移除传统菜单的左侧图标空栏；快捷键在右侧独立对齐，勿扰显示真实勾选状态，分组标题显示当前角色数量。
- 托盘菜单复用应用深浅色主题和高对比度颜色，随 DPI 调整字体、间距与圆角。
- 保留原来的七个命令、原生键盘操作及自动关闭；无角色时禁用显示、隐藏和召回全部。

### Fixed

- 退出时显式释放托盘菜单、字体、窗口区域及主题订阅。
- 阴影测试改为与系统原生菜单策略比较，兼容未启用阴影的 CI 和远程桌面环境，不强制改变系统视觉设置。
- CI 测试异常写入 GitHub 检查注释，失败时可直接查看具体断言。
- 增加托盘命令、主题同步、勾选状态、Esc 关闭、屏幕边缘定位和 100%/150%/200% 布局检查。

## [2.1.0] - 2026-09-08

### Added

- 原生 WPF 控制中心：侧栏导航、角色卡片、添加角色选择窗、卡片菜单与就地快捷设置。
- 跟随系统、浅色和深色主题，四种强调色和减少动态效果选项；兼容原有 `desktop-v2.json`。
- 主题与配置回归测试，以及实际原生窗口加载、开关动画、下拉选择、最小化恢复和计时保持检查。

### Changed

- 统一角色设置、学习陪伴、动作编辑器、备份恢复和确认弹窗的颜色、字体、间距及控件样式；保留原生标题栏。
- 快捷调整大小即时预览，连续操作合并保存；关闭控制中心前保存最后一次修改。
- 控制中心添加角色及打开设置前的皮肤扫描移至后台，关闭窗口会取消未完成的控制中心扫描。
- 角色卡片增量更新，保留选中项；隐藏、最小化或切换到设置页时停止卡片预览定时器。
- 控件悬停、按压、开关与页面切换加入短过渡；滚轮滚动平滑化。减少动态效果时自动头像预览静止，手动动作预览和桌面宠物动画仍可使用。
- 详细设置打开时锁定控制中心内可能产生冲突的修改，避免两个窗口覆盖同一配置。

### Compatibility

- 不修改已有皮肤 PNG、动作参数或皮肤包格式；导入皮肤、备份、角色配置和学习逻辑保持兼容。

## [2.0.0] - 2026-09-08

### Added

- 最多 3 个独立桌面角色，可使用相同或不同皮肤；独立位置、大小、显示状态、点击穿透、行为和四边收纳。
- 角色管理窗口：添加、显示/隐藏、召回、设置及关闭单个角色。
- 应用级托盘、全局召回快捷键和共享学习计时，关闭角色不结束计时，不重复发出通知。
- `desktop-v2.json` 多角色配置，首次读取旧 `settings.json` 迁移且不改写旧文件；原子保存和上一版 `.bak` 备份。
- 可复现的素材性能探针、真实窗口采样、窗口生命周期和图片租约回收检查。

### Changed

- 同内容 PNG 共享冻结图片与帧缓存，活跃角色保留图片租约，关闭后释放；非活跃图片采用有界弱引用索引。
- 隐藏角色停用动画、自动行为、漫游与收纳定时器；共享计时仍运行。
- `Ctrl + Alt + Y` 召回全部角色，托盘双击切换全部角色显示状态。
- 删除皮肤时保护所有正在使用该皮肤的角色，包括隐藏角色。
- 常规与行为设置作用于当前角色，学习陪伴、勿扰和开机启动属于全局设置。

### Fixed

- 设置窗口打开失败时释放应用级设置占用标记。
- 退出前保存原显示状态，避免下次启动时所有角色被误记为隐藏。
- 配置保存失败不阻止退出清理；显示器变化后重新约束可见角色位置。

## [1.5.0] - 2026-09-07

### Added

- 皮肤动作配置编辑器：精灵图网格、点击定位、逐帧时长、动作开关与循环、16 个注视方向配置、播放预览和重置。
- 保存编辑后的导入皮肤、另存独立皮肤、导出 ZIP；随附皮肤仅能保存副本，PNG 原始字节不变。
- 备份与恢复窗口：历史版本/已删除筛选、目录时间、占用大小、预览、恢复、恢复为副本、导出和单份永久清理。
- 支持从有效备份恢复损坏的用户皮肤目录，覆盖前保留当前目录，原备份不消耗。
- 编辑器控件、保存导出、恢复确认边界、目录联接保护和清理范围测试。

### Changed

- 保持 schemaVersion 1，与已有皮肤和 v1.4 的备份目录兼容。
- 皮肤保存、恢复仍在设置页保存后应用到桌面；未保存的编辑器草稿可放弃。

## [1.4.0] - 2026-09-07

### Fixed

- 刷新皮肤时，同 ID 且 JSON/PNG 完全相同的随附与历史导入副本不再误报冲突；保留原文件，同 ID 内容不同仍提示位置。

### Added

- 皮肤缩略图列表、单角色 ZIP 直接导入，以及保留原始 JSON/PNG 字节的 ZIP 导出。
- 导入皮肤的同 ID 更新、旧版本备份和删除确认；随附皮肤及当前使用皮肤的删除保护。
- 学习状态动作联动：专注时工作，暂停与休息时待机，完成时约 3 秒挥手或跳跃反馈。
- 学习面板动画头像、最小化暂停绘制、皮肤切换时重建帧缓存。
- ZIP 路径与大小限制、更新与删除保护、状态联动和头像动画回归测试。

### Changed

- 保持 schemaVersion 1 兼容；所有已有精灵图不变，未提供相应动作时回退待机。
- 完成动作遵守勿扰、通知和隐藏状态，不打断拖动或手动动作；忙碌时最多等待 10 秒，过期不补发。
- README、皮肤格式文档与 AI 制作教程补充 ZIP 导入、更新、分享和备份恢复流程。

## [1.3.0] - 2026-09-07

### Added

- 单击打招呼、双击跳跃；拖动使用系统阈值并兼容 DPI 缩放。
- 鼠标靠近时暂停自动走动，离开后继续，距离可调。
- 自动走动在显示器工作区边缘转向。
- 独立学习陪伴面板：专注和休息计时、暂停、继续、结束、跳过休息及本次完成次数。
- 学习陪伴面板支持最小化到任务栏，后台计时继续。
- 四边贴边自动隐藏：滑入收纳、悬停展开、离开延时收回、拖离解除停靠。
- 停靠把手在点击穿透模式下仍可操作，支持工作区边界、外接屏恢复和召回。
- 收纳状态保留计时，完成通知不强制展开，保存位置采用完整角色坐标。
- 可配置时长、定时休息提醒、手动勿扰与跨午夜定时勿扰。
- 专注时默认暂停自动动作，完成时通过托盘通知与现有角色动作反馈。
- 计时、提醒、勿扰时段、移动边界及控件事件回归测试。

### Changed

- 自动行为按到期时间调度，注视鼠标不再持续延后走动或随机动作。
- 提醒不自动抢焦点，勿扰期间丢弃过期通知，番茄钟期间避免重复休息提醒。
- 保持 v1.2.0 皮肤格式，缺少互动动作的皮肤自动回退。

### Fixed

- 贴边松手后完整收起，修复把手出现在鼠标下方导致收起动画被反向展开打断的问题。
- 收起后只有鼠标离开并重新进入把手才触发悬停展开，点击和菜单操作仍可直接展开。
- 初次收起从松手位置连续滑入，避免越界拖放先被纠正回屏幕后再收起的跳动。
- 位置保存延后到收起或展开动画结束，减少松手阶段的同步磁盘写入。

## [1.2.0] - 2026-09-07

### Added

- 外部本地皮肤包：`pet.json` 与 `spritesheet.png`，支持自定义尺寸、行列、动作时长和注视帧。
- 设置中的皮肤选择、动作预览、导入、刷新和用户皮肤目录入口。
- 皮肤格式及图像校验、重复导入保护、无效所选皮肤回退。
- 旧设置自动升级、皮肤能力适配，以及导入和切换回归测试。
- 面向用户的 AI 角色制作教程，包含分阶段提示词、动作布局、验收步骤和导入排错。

### Changed

- 默认叶瞬光精灵图移至 `Pets/YeShunguang`，文件字节保持不变。
- 移除程序内固定帧表与嵌入精灵图，切换时清空帧缓存并更新角色名称。
- ZIP 随附外置 `Pets` 目录，升级和移动时须保留完整目录。

## [1.1.0] - 2026-09-07

### Added

- 常规、行为和关于设置页面。
- 可调随机待机动作，默认开启。
- 可调桌面横向走动，默认关闭。
- `Ctrl + Alt + Y` 全局召回快捷键；召回时自动关闭点击穿透。
- 轮转大小受限的纯本地运行日志及日志目录入口。

### Changed

- 右键菜单和托盘菜单增加设置、召回及行为快捷开关。
- 多显示器工作区用于约束自动走动范围。
- `main` 和拉取请求增加轻量编译检查。
- 程序版本升级为 1.1.0。

## [1.0.1] - 2026-09-04

### Fixed

- 使用系统窗口拖动，修复高 DPI 缩放下的拖动偏移。
- 启动、缩放和拖动结束时将窗口约束在最近显示器的工作区内。
- 阻止重复启动多个实例；再次运行会显示已有窗口。
- 从注册表读取真实开机启动状态，移动 EXE 后不再显示错误状态。
- 使用临时文件替换方式保存设置，降低配置写入中断导致损坏的概率。

### Added

- 自定义应用和托盘图标。
- README 待机动画预览。
- 程序版本、作者、产品描述和仓库元数据。
- 便携版 ZIP、SHA256 打包脚本和标签发布工作流。
- 发布前固定校验原始精灵图 SHA256，防止素材被意外改动。
- 安装、卸载、故障排查、代码许可和素材权利说明。

## [1.0.0] - 2026-09-03

- 首个 Windows WPF 自包含 MVP。
