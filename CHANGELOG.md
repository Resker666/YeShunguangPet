# Changelog

本项目遵循语义化版本号。

## [2.1.1] - 2026-09-08

### Changed

- 托盘右键菜单改为圆角、轻描边和系统阴影的分组菜单，统一文字对齐、行高与柔和悬停高亮。
- 移除传统菜单的左侧图标空栏；快捷键在右侧独立对齐，勿扰显示真实勾选状态，分组标题显示当前角色数量。
- 托盘菜单复用应用深浅色主题和高对比度颜色，随 DPI 调整字体、间距与圆角。
- 保留原来的七个命令、原生键盘操作及自动关闭；无角色时禁用显示、隐藏和召回全部。

### Fixed

- 退出时显式释放托盘菜单、字体、窗口区域及主题订阅。
- 增加托盘命令、主题同步、勾选状态、Esc 关闭、屏幕边缘定位和 100%/150%/200% 布局检查。

## [2.1.0] - 2026-09-08

### Added

- 原生 WPF 控制中心：侧栏导航、角色卡片、添加角色选择窗、卡片菜单与就地快捷设置。
- 跟随系统、浅色和深色主题，四种强调色和减少动态效果选项；兼容原有 `desktop-v2.json`。
- 主题与配置回归测试，以及实际原生窗口加载、开关动画、下拉选择、最小化恢复和计时保持检查。

### Changed

- 统一角色设置、学习陪伴、动作编辑器、备份恢复和确认弹窗的颜色、字体、间距及控件样式；保留原生标题栏。
- 快捷调整大小即时预览，连续操作合并保存；关闭控制中心前保存最后一次修改。
- 控制中心添加角色及打开设置前的皮肤扫描移至后台，关闭窗口会取消未完成的控制中心扫描。
- 角色卡片增量更新，保留选中项；隐藏、最小化或切换到设置页时停止卡片预览定时器。
- 控件悬停、按压、开关与页面切换加入短过渡；滚轮滚动平滑化。减少动态效果时自动头像预览静止，手动动作预览和桌面宠物动画仍可使用。
- 详细设置打开时锁定控制中心内可能产生冲突的修改，避免两个窗口覆盖同一配置。

### Compatibility

- 不修改已有皮肤 PNG、动作参数或皮肤包格式；导入皮肤、备份、角色配置和学习逻辑保持兼容。

## [2.0.0] - 2026-09-08

### Added

- 最多 3 个独立桌面角色，可使用相同或不同皮肤；独立位置、大小、显示状态、点击穿透、行为和四边收纳。
- 角色管理窗口：添加、显示/隐藏、召回、设置及关闭单个角色。
- 应用级托盘、全局召回快捷键和共享学习计时，关闭角色不结束计时，不重复发出通知。
- `desktop-v2.json` 多角色配置，首次读取旧 `settings.json` 迁移且不改写旧文件；原子保存和上一版 `.bak` 备份。
- 可复现的素材性能探针、真实窗口采样、窗口生命周期和图片租约回收检查。

### Changed

- 同内容 PNG 共享冻结图片与帧缓存，活跃角色保留图片租约，关闭后释放；非活跃图片采用有界弱引用索引。
- 隐藏角色停用动画、自动行为、漫游与收纳定时器；共享计时仍运行。
- `Ctrl + Alt + Y` 召回全部角色，托盘双击切换全部角色显示状态。
- 删除皮肤时保护所有正在使用该皮肤的角色，包括隐藏角色。
- 常规与行为设置作用于当前角色，学习陪伴、勿扰和开机启动属于全局设置。

### Fixed

- 设置窗口打开失败时释放应用级设置占用标记。
- 退出前保存原显示状态，避免下次启动时所有角色被误记为隐藏。
- 配置保存失败不阻止退出清理；显示器变化后重新约束可见角色位置。

## [1.5.0] - 2026-09-07

### Added

- 皮肤动作配置编辑器：精灵图网格、点击定位、逐帧时长、动作开关与循环、16 个注视方向配置、播放预览和重置。
- 保存编辑后的导入皮肤、另存独立皮肤、导出 ZIP；随附皮肤仅能保存副本，PNG 原始字节不变。
- 备份与恢复窗口：历史版本/已删除筛选、目录时间、占用大小、预览、恢复、恢复为副本、导出和单份永久清理。
- 支持从有效备份恢复损坏的用户皮肤目录，覆盖前保留当前目录，原备份不消耗。
- 编辑器控件、保存导出、恢复确认边界、目录联接保护和清理范围测试。

### Changed

- 保持 schemaVersion 1，与已有皮肤和 v1.4 的备份目录兼容。
- 皮肤保存、恢复仍在设置页保存后应用到桌面；未保存的编辑器草稿可放弃。

## [1.4.0] - 2026-09-07

### Fixed

- 刷新皮肤时，同 ID 且 JSON/PNG 完全相同的随附与历史导入副本不再误报冲突；保留原文件，同 ID 内容不同仍提示位置。

### Added

- 皮肤缩略图列表、单角色 ZIP 直接导入，以及保留原始 JSON/PNG 字节的 ZIP 导出。
- 导入皮肤的同 ID 更新、旧版本备份和删除确认；随附皮肤及当前使用皮肤的删除保护。
- 学习状态动作联动：专注时工作，暂停与休息时待机，完成时约 3 秒挥手或跳跃反馈。
- 学习面板动画头像、最小化暂停绘制、皮肤切换时重建帧缓存。
- ZIP 路径与大小限制、更新与删除保护、状态联动和头像动画回归测试。

### Changed

- 保持 schemaVersion 1 兼容；所有已有精灵图不变，未提供相应动作时回退待机。
- 完成动作遵守勿扰、通知和隐藏状态，不打断拖动或手动动作；忙碌时最多等待 10 秒，过期不补发。
- README、皮肤格式文档与 AI 制作教程补充 ZIP 导入、更新、分享和备份恢复流程。

## [1.3.0] - 2026-09-07

### Added

- 单击打招呼、双击跳跃；拖动使用系统阈值并兼容 DPI 缩放。
- 鼠标靠近时暂停自动走动，离开后继续，距离可调。
- 自动走动在显示器工作区边缘转向。
- 独立学习陪伴面板：专注和休息计时、暂停、继续、结束、跳过休息及本次完成次数。
- 学习陪伴面板支持最小化到任务栏，后台计时继续。
- 四边贴边自动隐藏：滑入收纳、悬停展开、离开延时收回、拖离解除停靠。
- 停靠把手在点击穿透模式下仍可操作，支持工作区边界、外接屏恢复和召回。
- 收纳状态保留计时，完成通知不强制展开，保存位置采用完整角色坐标。
- 可配置时长、定时休息提醒、手动勿扰与跨午夜定时勿扰。
- 专注时默认暂停自动动作，完成时通过托盘通知与现有角色动作反馈。
- 计时、提醒、勿扰时段、移动边界及控件事件回归测试。

### Changed

- 自动行为按到期时间调度，注视鼠标不再持续延后走动或随机动作。
- 提醒不自动抢焦点，勿扰期间丢弃过期通知，番茄钟期间避免重复休息提醒。
- 保持 v1.2.0 皮肤格式，缺少互动动作的皮肤自动回退。

### Fixed

- 贴边松手后完整收起，修复把手出现在鼠标下方导致收起动画被反向展开打断的问题。
- 收起后只有鼠标离开并重新进入把手才触发悬停展开，点击和菜单操作仍可直接展开。
- 初次收起从松手位置连续滑入，避免越界拖放先被纠正回屏幕后再收起的跳动。
- 位置保存延后到收起或展开动画结束，减少松手阶段的同步磁盘写入。

## [1.2.0] - 2026-09-07

### Added

- 外部本地皮肤包：`pet.json` 与 `spritesheet.png`，支持自定义尺寸、行列、动作时长和注视帧。
- 设置中的皮肤选择、动作预览、导入、刷新和用户皮肤目录入口。
- 皮肤格式及图像校验、重复导入保护、无效所选皮肤回退。
- 旧设置自动升级、皮肤能力适配，以及导入和切换回归测试。
- 面向用户的 AI 角色制作教程，包含分阶段提示词、动作布局、验收步骤和导入排错。

### Changed

- 默认叶瞬光精灵图移至 `Pets/YeShunguang`，文件字节保持不变。
- 移除程序内固定帧表与嵌入精灵图，切换时清空帧缓存并更新角色名称。
- ZIP 随附外置 `Pets` 目录，升级和移动时须保留完整目录。

## [1.1.0] - 2026-09-07

### Added

- 常规、行为和关于设置页面。
- 可调随机待机动作，默认开启。
- 可调桌面横向走动，默认关闭。
- `Ctrl + Alt + Y` 全局召回快捷键；召回时自动关闭点击穿透。
- 轮转大小受限的纯本地运行日志及日志目录入口。

### Changed

- 右键菜单和托盘菜单增加设置、召回及行为快捷开关。
- 多显示器工作区用于约束自动走动范围。
- `main` 和拉取请求增加轻量编译检查。
- 程序版本升级为 1.1.0。

## [1.0.1] - 2026-09-04

### Fixed

- 使用系统窗口拖动，修复高 DPI 缩放下的拖动偏移。
- 启动、缩放和拖动结束时将窗口约束在最近显示器的工作区内。
- 阻止重复启动多个实例；再次运行会显示已有窗口。
- 从注册表读取真实开机启动状态，移动 EXE 后不再显示错误状态。
- 使用临时文件替换方式保存设置，降低配置写入中断导致损坏的概率。

### Added

- 自定义应用和托盘图标。
- README 待机动画预览。
- 程序版本、作者、产品描述和仓库元数据。
- 便携版 ZIP、SHA256 打包脚本和标签发布工作流。
- 发布前固定校验原始精灵图 SHA256，防止素材被意外改动。
- 安装、卸载、故障排查、代码许可和素材权利说明。

## [1.0.0] - 2026-09-03

- 首个 Windows WPF 自包含 MVP。
