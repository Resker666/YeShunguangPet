<ui:ThemedWindow x:Class="YeShunguangPet.CapturePinEditToolbarWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:YeShunguangPet" Title="贴图编辑工具" Width="640" SizeToContent="Height"
    WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent"
    ShowInTaskbar="False" ShowActivated="True" Topmost="True" WindowStartupLocation="Manual" PreviewKeyDown="Toolbar_KeyDown">
    <ui:ThemedWindow.Resources>
        <Style x:Key="Tool" TargetType="RadioButton" BasedOn="{StaticResource SegmentButton}"><Setter Property="Width" Value="38"/><Setter Property="Height" Value="40"/><Setter Property="MinHeight" Value="40"/><Setter Property="FontSize" Value="18"/><Setter Property="Padding" Value="0"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/></Style>
        <Style x:Key="Command" TargetType="Button" BasedOn="{StaticResource IconButton}"><Setter Property="Width" Value="38"/><Setter Property="Height" Value="40"/><Setter Property="MinHeight" Value="40"/><Setter Property="FontSize" Value="18"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/></Style>
        <Style x:Key="ToolGroup" TargetType="Border"><Setter Property="Background" Value="Transparent"/><Setter Property="BorderBrush" Value="{DynamicResource SwitchOffBrush}"/><Setter Property="BorderThickness" Value="0,0,1,0"/><Setter Property="CornerRadius" Value="0"/><Setter Property="Padding" Value="0,0,8,0"/><Setter Property="Margin" Value="0,0,8,0"/></Style>
        <Style x:Key="InkSwatch" TargetType="RadioButton" BasedOn="{StaticResource SegmentButton}">
            <Setter Property="Width" Value="26"/><Setter Property="Height" Value="26"/><Setter Property="MinHeight" Value="26"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RadioButton">
                <Grid><Ellipse x:Name="Ring" Stroke="Transparent" StrokeThickness="1"/><Ellipse Margin="4" Fill="{TemplateBinding Background}" Stroke="{DynamicResource SecondaryTextBrush}" StrokeThickness="0.5"/>
                    <TextBlock x:Name="Check" FontFamily="{StaticResource IconFont}" Text="&#xE73E;" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Hidden"/>
                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsChecked" Value="True"><Setter TargetName="Ring" Property="Stroke" Value="{DynamicResource AccentTextBrush}"/><Setter TargetName="Check" Property="Visibility" Value="Visible"/></Trigger>
                    <Trigger Property="Tag" Value="#FFFFFF"><Setter TargetName="Check" Property="Foreground" Value="Black"/></Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Ring" Property="Stroke" Value="{DynamicResource TextBrush}"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
    </ui:ThemedWindow.Resources>
    <Border x:Name="GlassSurface" Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8,4">
        <Border.Effect><DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.18"/></Border.Effect>
        <StackPanel>
            <WrapPanel x:Name="ToolOptions" Visibility="Collapsed" Margin="0,0,0,6">
                <WrapPanel x:Name="ColorOptions">
                    <RadioButton x:Name="RedSwatch" Style="{StaticResource InkSwatch}" GroupName="Color" Tag="#D62F4B" Background="#D62F4B" ToolTip="红色" AutomationProperties.Name="红色" Checked="Color_Changed"/>
                    <RadioButton Style="{StaticResource InkSwatch}" GroupName="Color" Tag="#287A5C" Background="#287A5C" ToolTip="绿色" AutomationProperties.Name="绿色" Checked="Color_Changed"/>
                    <RadioButton Style="{StaticResource InkSwatch}" GroupName="Color" Tag="#396BB8" Background="#396BB8" ToolTip="蓝色" AutomationProperties.Name="蓝色" Checked="Color_Changed"/>
                    <RadioButton Style="{StaticResource InkSwatch}" GroupName="Color" Tag="#FFFFFF" Background="#FFFFFF" ToolTip="白色" AutomationProperties.Name="白色" Checked="Color_Changed"/>
                    <RadioButton Style="{StaticResource InkSwatch}" GroupName="Color" Tag="#111111" Background="#111111" ToolTip="黑色" AutomationProperties.Name="黑色" Checked="Color_Changed"/>
                </WrapPanel>
                <StackPanel x:Name="StrokeOptions" Orientation="Horizontal" Margin="10,0,0,0">
                    <TextBlock Text="线宽" FontSize="12" VerticalAlignment="Center"/>
                    <Slider x:Name="LineWidth" Width="82" Minimum="1" Maximum="16" Value="4" IsSnapToTickEnabled="True" TickFrequency="1" AutomationProperties.Name="线宽" ValueChanged="Options_Changed"/>
                </StackPanel>
                <StackPanel x:Name="FontOptions" Orientation="Horizontal" Margin="10,0,0,0">
                    <TextBlock Text="字号" FontSize="12" VerticalAlignment="Center"/>
                    <Slider x:Name="TextSize" Width="72" Minimum="12" Maximum="64" Value="22" IsSnapToTickEnabled="True" TickFrequency="2" AutomationProperties.Name="字号" ValueChanged="Options_Changed"/>
                    <TextBox x:Name="LabelInput" Width="140" MaxLength="500" Margin="8,0,0,0" ToolTip="标注文字" AutomationProperties.Name="标注文字" TextChanged="Label_Changed" LostKeyboardFocus="Label_Commit" KeyDown="Label_KeyDown"/>
                </StackPanel>
            </WrapPanel>
            <WrapPanel x:Name="CommandRow" HorizontalAlignment="Center">
                <Border Style="{StaticResource ToolGroup}"><WrapPanel>
                    <RadioButton x:Name="SelectTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Select" ToolTip="选择标注" AutomationProperties.Name="选择标注" Click="Tool_Click"><TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE7C9;"/></RadioButton>
                    <RadioButton x:Name="CropTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Crop" ToolTip="裁剪" AutomationProperties.Name="裁剪" Click="Tool_Click"><TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE7A8;"/></RadioButton>
                    <RadioButton x:Name="RectangleTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Rectangle" ToolTip="矩形" AutomationProperties.Name="矩形" Click="Tool_Click"><TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE739;"/></RadioButton>
                    <RadioButton x:Name="EllipseTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Ellipse" ToolTip="椭圆" AutomationProperties.Name="椭圆" Click="Tool_Click"><TextBlock FontFamily="{StaticResource IconFont}" Text="&#xEA3A;"/></RadioButton>
                    <RadioButton x:Name="ArrowTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Arrow" ToolTip="箭头" AutomationProperties.Name="箭头" Click="Tool_Click"><TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE72A;"/></RadioButton>
                    <RadioButton x:Name="PenTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Pen" ToolTip="画笔" AutomationProperties.Name="画笔" Click="Tool_Click"><TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE70F;"/></RadioButton>
                    <RadioButton x:Name="TextTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Text" ToolTip="文字" AutomationProperties.Name="文字" Click="Tool_Click"><Border Width="17" Height="17" BorderBrush="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}" BorderThickness="1"><TextBlock Text="T" FontFamily="Segoe UI" FontWeight="SemiBold" FontSize="12" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></RadioButton>
                    <RadioButton x:Name="MosaicTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Mosaic" ToolTip="马赛克遮挡" AutomationProperties.Name="马赛克遮挡" Click="Tool_Click"><Border Width="17" Height="17" BorderBrush="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}" BorderThickness="1" Padding="2"><UniformGrid Rows="3" Columns="3"><Rectangle Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/><Rectangle Fill="Transparent"/><Rectangle Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/><Rectangle Fill="Transparent"/><Rectangle Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/><Rectangle Fill="Transparent"/><Rectangle Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/><Rectangle Fill="Transparent"/><Rectangle Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/></UniformGrid></Border></RadioButton>
                    <RadioButton x:Name="RedactTool" Style="{StaticResource Tool}" GroupName="Mode" Tag="Redact" ToolTip="安全遮挡" AutomationProperties.Name="安全遮挡" Click="Tool_Click"><Border Width="17" Height="13" Background="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/></RadioButton>
                </WrapPanel></Border>
                <Border Style="{StaticResource ToolGroup}"><StackPanel Orientation="Horizontal">
                    <Button x:Name="UndoButton" Style="{StaticResource Command}" Content="&#xE7A7;" ToolTip="撤销" AutomationProperties.Name="撤销" Click="Undo_Click"/>
                    <Button x:Name="RedoButton" Style="{StaticResource Command}" Content="&#xE7A6;" ToolTip="重做" AutomationProperties.Name="重做" Click="Redo_Click"/>
                    <Button x:Name="DeleteButton" Style="{StaticResource Command}" Content="&#xE74D;" ToolTip="删除标注" AutomationProperties.Name="删除标注" Click="Delete_Click"/>
                </StackPanel></Border>
                <Border Style="{StaticResource ToolGroup}" Margin="0" Padding="0" BorderThickness="0"><StackPanel Orientation="Horizontal">
                    <Button x:Name="CancelButton" Style="{StaticResource Command}" Content="&#xE711;" Foreground="{DynamicResource DangerBrush}" ToolTip="取消编辑" AutomationProperties.Name="取消编辑" Click="Cancel_Click"/>
                    <Button x:Name="DoneButton" Style="{StaticResource Command}" Content="&#xE73E;" Foreground="#239D62" ToolTip="完成编辑" AutomationProperties.Name="完成编辑" Click="Done_Click"/>
                </StackPanel></Border>
            </WrapPanel>
            <TextBlock x:Name="ErrorText" TextWrapping="Wrap" MaxHeight="48" FontSize="12" Foreground="{DynamicResource DangerBrush}" Visibility="Collapsed" Margin="4,6,4,0" AutomationProperties.LiveSetting="Polite"/>
        </StackPanel>
    </Border>
</ui:ThemedWindow>
